th=0.05;
corr=0.01
t=1:100000;
x1=rand([length(t) 1]);
ind=find(x1>th);
x1(ind)=1;
x1=fix(x1);
plot(x1);

x2=rand([length(t) 1]);
ind2=find(x2>th);
x2(ind2)=1;
x2=fix(x2);

xHi=rand([length(ind) 1]);
